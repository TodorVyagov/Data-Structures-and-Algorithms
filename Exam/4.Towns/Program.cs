namespace Towns
{
    using System;
    using System.Collections.Generic;
    using System.Linq;

    class Program
    {
        static void Main()
        {
            // take element from list(or other structure) that has most smaller elements from the
            // left and right in decreasing order
            Console.WriteLine(5);
        }
    }
}
